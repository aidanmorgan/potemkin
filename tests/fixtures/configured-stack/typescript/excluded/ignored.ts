// This file must never be evaluated. The configuration excludes **/excluded/**/*.ts.
// The deliberately broken source lets the discovery test prove that exclusions
// are applied before TypeScript compilation.
export const invalid = ;
