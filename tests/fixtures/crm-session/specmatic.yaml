version: 3
systemUnderTest:
  service:
    definitions:
      - source: { fileSystem: { directory: ./openapi } }
        specs:
          - id: crm-session
            path: crm-session.yaml
            urlPathPrefix: /
components:
  dictionaries:
    crm-defaults:
      path: ./dictionary.yaml
