version: 3
systemUnderTest:
  service:
    definitions:
      - source: { fileSystem: { directory: ./openapi } }
        specs:
          - id: crm-versioned
            path: nuisance-bureau.yaml
            urlPathPrefix: /
components:
  dictionaries:
    crm-defaults:
      path: ./dictionary.yaml
