version: 3
systemUnderTest:
  service:
    definitions:
      - source: { fileSystem: { directory: ./openapi } }
        specs:
          - id: stripe-official
            path: stripe-official.json
            urlPathPrefix: /
